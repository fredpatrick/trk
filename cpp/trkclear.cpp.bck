#include <iostream>
#include "layoutconfig.h"

using namespace trk;

int main() {

    std::cout << "BeagleBoneBlack utility clearing exported gpios" <<std::endl;

    LayoutConfig* layout_config = LayoutConfig::instance();
    layout_config->clear_gpios();

    return 0;
}

